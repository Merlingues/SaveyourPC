#!/bin/bash

if [[ "$*" == *"/"* ]] || [[ "$*" == *".."* ]]; then
    echo "Accès refusé : Vous n'avez pas les permissions pour quitter /home/joueur."
    exit 1
fi

/bin/ls --color=auto /home/joueur "$@"